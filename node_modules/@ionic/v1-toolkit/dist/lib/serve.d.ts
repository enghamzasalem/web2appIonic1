import * as ζproxyMiddleware from 'http-proxy-middleware';
import { ConfigFileProxy } from './config';
export declare const WATCH_PATTERNS: string[];
export declare function proxyConfigToMiddlewareConfig(proxy: ConfigFileProxy): ζproxyMiddleware.Config;
export interface ProxyConfig extends ζproxyMiddleware.Config {
    mount: string;
}
export interface ServeOptions {
    host: string;
    port: number;
    livereload: boolean;
    consolelogs: boolean;
    devPort: number;
    livereloadPort: number;
    wwwDir: string;
    engine: string;
    platform?: string;
    watchPatterns: string[];
    proxies: ProxyConfig[];
}
export declare function runServer(options: ServeOptions): Promise<ServeOptions>;
